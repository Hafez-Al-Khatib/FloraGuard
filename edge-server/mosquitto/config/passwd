# Placeholder password file for Mosquitto.
# Generate real entries with:
#   docker run -it --rm -v "$PWD/mosquitto/config:/mosquitto/config" eclipse-mosquitto:2 \
#     mosquitto_passwd -b /mosquitto/config/passwd <username> <password>
